# Internship-internspark
Built a Loan Approval Prediction project using Python and Machine Learning. Performed data preprocessing, handled missing values, encoded categorical features, scaled data, trained and evaluated multiple models, and analyzed performance using accuracy, precision, recall, F1-score, and ROC-AUC.
